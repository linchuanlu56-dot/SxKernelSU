version: "3.9"
services:
  dev:
    build: .
    volumes:
      - .:/workspace
      - cargo-cache:/usr/local/cargo/registry
    working_dir: /workspace
    command: tail -f /dev/null
    stdin_open: true
    tty: true

volumes:
  cargo-cache:
